"use server";import {revalidatePath} from "next/cache";import {getWorkspace,hasAnyRole} from "../../lib/workspace";const r=(fd:FormData,k:string)=>String(fd.get(k)||"").trim();export async function updateTrack(fd:FormData){const {supabase,project,roles}=await getWorkspace();if(!project||!hasAnyRole(roles,["Super Admin","Project Lead","A&R","Producer","Engineer"]))throw new Error("Your role cannot update track development.");const status=r(fd,"status");if(!["in_development","revision","in_studio","mixing","mastering","release_ready","complete"].includes(status))throw new Error("Invalid track status.");const {error}=await supabase.from("tracks").update({development_status:status,status,updated_at:new Date().toISOString()}).eq("id",r(fd,"track_id")).eq("project_id",project.id);if(error)throw new Error(error.message);revalidatePath("/tracks");}
export async function commentOnTrack(fd:FormData){const {supabase,user,project,membership,roles}=await getWorkspace();if(!project||!membership)throw new Error("Project access required.");const body=r(fd,"comment");if(!body)return;const kind=roles.includes("A&R")?"ar_note":"comment";const {error}=await supabase.from("comments").insert({project_id:project.id,entity_type:"track",entity_id:r(fd,"track_id"),user_id:user.id,kind,body});if(error)throw new Error(error.message);revalidatePath("/tracks");}
