import AppShell from"../../components/AppShell";import{getWorkspace}from"../../lib/workspace";import{addProjectExpense,addRevenueRecord,saveProjectBudget}from"./actions";
export default async function FinancePage(){const{admin,project,membership}=await getWorkspace();if(!project||!membership)return<AppShell><div className="content empty-state"><h2>Project access required</h2></div></AppShell>;const[budgetResult,expensesResult,revenueResult,tracksResult,oppsResult]=await Promise.all([admin.from("project_budgets").select("*").eq("project_id",project.id).maybeSingle(),admin.from("project_expenses").select("*,tracks(working_title)").eq("project_id",project.id).order("expense_date",{ascending:false}).limit(50),admin.from("revenue_records").select("*,tracks(working_title)").eq("project_id",project.id).order("created_at",{ascending:false}).limit(50),admin.from("tracks").select("id,working_title").eq("project_id",project.id).order("working_title"),admin.from("commercial_opportunities").select("id,organisation,opportunity_type").eq("project_id",project.id).order("created_at",{ascending:false})]);const budget=budgetResult.data,expenses=expensesResult.data||[],revenue=revenueResult.data||[],tracks=tracksResult.data||[],opportunities=oppsResult.data||[];const paid=expenses.filter((item:any)=>item.payment_status==="paid").reduce((sum:number,item:any)=>sum+Number(item.amount||0),0),committed=expenses.filter((item:any)=>item.payment_status==="committed").reduce((sum:number,item:any)=>sum+Number(item.amount||0),0),received=revenue.filter((item:any)=>item.payment_status==="paid").reduce((sum:number,item:any)=>sum+Number(item.amount||0),0),budgetAmount=Number(budget?.budget_amount||0),currency=budget?.currency||"KES";return<AppShell><div className="content operations-page"><div className="heading"><div><span className="eyebrow">{project.code} / ECONOMICS</span><h1>Project Finance</h1><p>Basic operational control—budget, committed spend, actual spend and received revenue. Not accounting software.</p></div></div><section className="finance-metrics">{[["Project Budget",budgetAmount],["Actual Spend",paid],["Committed",committed],["Remaining",budgetAmount-paid-committed],["Revenue Received",received]].map(([label,value])=><article key={String(label)}><span>{label}</span><strong>{currency} {Number(value).toLocaleString()}</strong></article>)}</section><div className="platform-home-split"><details className="beat-intake-disclosure" open><summary className="beat-intake-summary"><span>BUDGET CONTROL</span><strong>Budget & expense</strong><b>Open +</b></summary><form action={saveProjectBudget} className="panel operations-form"><input name="budget_amount" type="number" min="0" step="0.01" defaultValue={budgetAmount}/><select name="currency" defaultValue={currency}><option>KES</option><option>USD</option><option>EUR</option></select><button>Save budget</button></form><form action={addProjectExpense} className="panel operations-form"><input name="amount" type="number" min="0.01" step="0.01" placeholder="Expense amount" required/><select name="currency" defaultValue={currency}><option>KES</option><option>USD</option></select><input name="category" required placeholder="Category"/><input name="vendor" placeholder="Vendor / person"/><input name="expense_date" type="date"/><select name="payment_status" defaultValue="committed"><option value="committed">Committed</option><option value="paid">Paid</option><option value="cancelled">Cancelled</option></select><select name="track_id" defaultValue=""><option value="">Project-wide</option>{tracks.map((track:any)=><option key={track.id} value={track.id}>{track.working_title}</option>)}</select><textarea name="notes" placeholder="Notes"/><button>Add expense</button></form></details><details className="beat-intake-disclosure" open><summary className="beat-intake-summary"><span>REVENUE CONTROL</span><strong>Record real revenue</strong><b>Open +</b></summary><form action={addRevenueRecord} className="panel operations-form"><input name="revenue_source" required placeholder="Revenue source"/><input name="amount" type="number" min="0" step="0.01" required placeholder="Amount"/><select name="currency" defaultValue={currency}><option>KES</option><option>USD</option></select><select name="payment_status" defaultValue="expected"><option value="expected">Expected</option><option value="invoiced">Invoiced</option><option value="partially_paid">Partially Paid</option><option value="paid">Paid</option><option value="overdue">Overdue</option><option value="cancelled">Cancelled</option></select><select name="track_id" defaultValue=""><option value="">Project-wide</option>{tracks.map((track:any)=><option key={track.id} value={track.id}>{track.working_title}</option>)}</select><select name="opportunity_id" defaultValue=""><option value="">No linked opportunity</option>{opportunities.map((item:any)=><option key={item.id} value={item.id}>{item.organisation||item.opportunity_type}</option>)}</select><input name="expected_date" type="date"/><input name="received_date" type="date"/><textarea name="notes" placeholder="Notes"/><button>Add revenue record</button></form></details></div><section className="platform-home-split"><article className="platform-home-section"><h2>Expense ledger</h2><div className="home-list">{!expenses.length&&<p>No expenses recorded.</p>}{expenses.map((item:any)=><div key={item.id}><time>{item.expense_date}</time><span><strong>{item.category} · {item.currency} {Number(item.amount).toLocaleString()}</strong><small>{item.vendor||"Project expense"} · {item.payment_status}</small></span></div>)}</div></article><article className="platform-home-section"><h2>Revenue ledger</h2><div className="home-list">{!revenue.length&&<p>No revenue recorded.</p>}{revenue.map((item:any)=><div key={item.id}><time>{item.received_date||item.expected_date||"Pending"}</time><span><strong>{item.revenue_source} · {item.currency} {Number(item.amount).toLocaleString()}</strong><small>{item.payment_status}</small></span></div>)}</div></article></section></div></AppShell>}
